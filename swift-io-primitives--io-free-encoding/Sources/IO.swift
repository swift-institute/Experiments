//
// IO — namespace for the algebraic theory of IO effects.
//
// Hosts the free-encoded program type (`IO.Free`), the per-operation
// inspection enum (`IO.Op`), the dictionary-encoded handler
// (`IO.Handler`), and the typed error (`IO.Error`).
//

public enum IO {}
