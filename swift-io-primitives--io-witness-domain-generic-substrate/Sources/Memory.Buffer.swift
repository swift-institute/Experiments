//
// Memory.Buffer — read-only byte buffer (placeholder for sketch).
//

extension Memory {
    public struct Buffer {
        public let count: Int

        public init(count: Int) {
            self.count = count
        }
    }
}
