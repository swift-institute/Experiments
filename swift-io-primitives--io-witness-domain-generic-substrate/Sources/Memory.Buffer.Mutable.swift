//
// Memory.Buffer.Mutable — mutable byte buffer (placeholder for sketch).
//

extension Memory.Buffer {
    public struct Mutable {
        public let count: Int

        public init(count: Int) {
            self.count = count
        }
    }
}
