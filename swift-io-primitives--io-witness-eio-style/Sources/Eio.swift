// Eio namespace — emulates OCaml Eio's `Stdenv.t` pattern in Swift. Hosts the
// three @Witness sub-capability structs (Net, File, Clock), the Stdenv bundle,
// and the `with(stdenv:_:)` scope entry point.

public enum Eio {}
