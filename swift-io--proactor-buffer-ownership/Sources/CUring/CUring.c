// Placeholder TU so SwiftPM has something to compile for the CUring
// target. The actual shim functions are all `static inline` in CUring.h
// (they compile into the experiment target directly).
#include "CUring.h"
